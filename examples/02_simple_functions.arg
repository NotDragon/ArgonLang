// Simple function declarations and definitions
func add(a: i32, b: i32) i32 {
    return a + b;
}

func multiply(x: f64, y: f64) f64 -> x * y;

func greet(name: str) str {
    return "Hello, " + name;
}

func isEven(n: i32) bool -> n % 2 == 0;

func main() i32 {
    def result: i32 = add(5, 3);
    def product: f64 = multiply(2.5, 4.0);
    def greeting: str = greet("World");
    def even: bool = isEven(42);
    
    return 0;
}
